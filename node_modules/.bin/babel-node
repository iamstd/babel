#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.6.14.0@babel-cli/bin/babel-node.js" "$@"
  ret=$?
else 
  node  "$basedir/../.6.14.0@babel-cli/bin/babel-node.js" "$@"
  ret=$?
fi
exit $ret
